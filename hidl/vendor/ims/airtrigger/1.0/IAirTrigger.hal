package vendor.ims.airtrigger@1.0;

interface IAirTrigger {

    getBarSqueezeConfig() generates (SqueezeConfig config);

    getBarTabConfig(uint32_t id) generates (TabConfig config);

    isEnabled(int32_t mode) generates (bool ret);

    oneway setBarSqueezeConfig(SqueezeConfig config);

    oneway setBarTabConfig(TabConfig config);

    oneway setEnable(int32_t mode, bool enable);
};